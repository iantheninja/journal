import { journalEntry } from './journal-entry';
import './styles.css';

$(document).ready(function(event) {
  console.log("pages are connected to one another");
  console.log(journalEntry());
    
});